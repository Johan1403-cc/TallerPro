# Routes

- `bd.js`: pool/conexión de SQL Server mediante `mssql`.
- `index.js`: shell de la aplicación y endpoint `/health`.
- `api.js`: API de TallerPro y operaciones con procedimientos almacenados.

La aplicación monta la API en `/api`.
