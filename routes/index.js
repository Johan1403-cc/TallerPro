const express=require('express');const modules=require('../config/modules');const {requireAuth,permit}=require('../middleware/auth');const {getPool,sql}=require('../config/db');const router=express.Router();
router.get('/',(req,res)=>res.redirect(req.user?'/dashboard':'/login'));
router.get('/login',(req,res)=>{if(req.user)return res.redirect('/dashboard');res.render('login',{layout:false});});
router.get('/dashboard',requireAuth,async(req,res,next)=>{try{const p=await getPool();const rs=await p.request().input('uid',sql.Int,req.user.id).query(`SELECT (SELECT COUNT(*) FROM Recepciones WHERE Estado NOT IN('ENTREGADA','CANCELADA')) VehiculosTaller,(SELECT COUNT(*) FROM OrdenesTrabajo WHERE Estado IN('REGISTRADA','PENDIENTE DE APROBACION','APROBADA','EN PROCESO','EN ESPERA DE REPUESTOS')) OrdenesPendientes,(SELECT COUNT(*) FROM OrdenesTrabajo WHERE FechaEstimadaEntrega<SYSDATETIME() AND Estado NOT IN('FINALIZADA','FACTURADA','ENTREGADA','CANCELADA')) OrdenesAtrasadas,(SELECT COUNT(*) FROM Citas WHERE CONVERT(date,FechaHoraInicio)=CONVERT(date,SYSDATETIME()) AND Estado NOT IN('CANCELADA')) CitasHoy,(SELECT ISNULL(SUM(Total),0) FROM Ventas WHERE CONVERT(date,FechaHora)=CONVERT(date,SYSDATETIME()) AND Estado='CONFIRMADA') VentasHoy,(SELECT ISNULL(SUM(Total),0) FROM Facturas WHERE YEAR(FechaHora)=YEAR(SYSDATETIME()) AND MONTH(FechaHora)=MONTH(SYSDATETIME()) AND Estado<>'ANULADA') IngresosMes,(SELECT COUNT(*) FROM Productos WHERE Activo=1 AND ExistenciaActual<=ExistenciaMinima) ProductosMinimos,(SELECT COUNT(*) FROM Facturas WHERE Estado IN('PENDIENTE','PARCIALMENTE PAGADA')) FacturasPendientes; SELECT TOP 6 FORMAT(FechaHora,'yyyy-MM') Mes,SUM(Total) Total FROM Ventas WHERE Estado='CONFIRMADA' GROUP BY FORMAT(FechaHora,'yyyy-MM') ORDER BY Mes DESC;`);res.render('dashboard',{title:'Panel principal',k:rs.recordsets[0][0],chart:rs.recordsets[1].reverse(),permissions:req.user.permissions});}catch(e){next(e)}});
router.get('/modulo/:name',requireAuth,(req,res)=>{const m=modules[req.params.name];if(!m)return res.status(404).render('error',{message:'Módulo no existe'});if(!req.user.permissions.includes(m.permission+'_CONSULTAR')&&!req.user.permissions.includes('ADMIN_TOTAL'))return res.status(403).render('error',{message:'No posee autorización.'});if(req.params.name==='vehiculos')return res.render('modules/vehiculos',{title:m.title});if(req.params.name==='citas')return res.render('modules/citas',{title:m.title});if(req.params.name==='recepciones')return res.render('modules/recepciones',{title:m.title});if(req.params.name==='diagnosticos')return res.render('modules/diagnosticos',{title:m.title});if(req.params.name==='cotizaciones')return res.render('modules/cotizaciones',{title:m.title});if(req.params.name==='ordenes')return res.render('modules/ordenes',{title:m.title});if(req.params.name==='garantias')return res.render('modules/garantias',{title:m.title});res.render('modules/crud',{title:m.title,moduleName:req.params.name,metaJson:JSON.stringify(m),fields:m.fields});});
router.get('/usuarios',requireAuth,permit('USUARIOS_ADMINISTRAR'),(req,res)=>res.render('modules/usuarios',{title:'Usuarios'}));
router.get('/roles',requireAuth,permit('USUARIOS_ADMINISTRAR'),(req,res)=>res.render('modules/roles',{title:'Roles'}));
router.get('/permisos',requireAuth,permit('USUARIOS_ADMINISTRAR'),(req,res)=>res.render('modules/permisos',{title:'Permisos'}));
router.get('/inventario',requireAuth,permit('INVENTARIO_CONSULTAR'),(req,res)=>res.render('modules/inventario',{title:'Inventario'}));
router.get('/compras',requireAuth,permit('COMPRAS_CONSULTAR'),(req,res)=>res.render('modules/compras',{title:'Compras'}));
router.get('/ventas',requireAuth,permit('VENTAS_CONSULTAR'),(req,res)=>res.render('modules/ventas',{title:'Ventas'}));
router.get('/facturacion',requireAuth,permit('FACTURAS_CONSULTAR'),(req,res)=>res.render('modules/facturacion',{title:'Facturación'}));
router.get('/pagos',requireAuth,permit('PAGOS_CONSULTAR'),(req,res)=>res.render('modules/pagos',{title:'Pagos'}));
router.get('/reportes',requireAuth,permit('REPORTES_CONSULTAR'),(req,res)=>res.render('modules/reportes',{title:'Reportes'}));
router.get('/auditoria',requireAuth,permit('AUDITORIA_CONSULTAR'),(req,res)=>res.render('modules/auditoria',{title:'Auditoría'}));
router.get('/notificaciones',requireAuth,(req,res)=>res.render('modules/notificaciones',{title:'Notificaciones'}));
module.exports=router;
